version: "3.5"
services:
  db:
    image: "postgres:11-alpine"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
  app:
    image: "library"
    ports:
      - "8080:8080"
    depends_on:
      - db